// Example unit test for waitForMSG
const waitForMSG = require('../src/wait');

describe('waitForMSG', () => {
  it('should be a function', () => {
    expect(typeof waitForMSG).toBe('function');
  });
  // Add more tests as implementation allows
});
